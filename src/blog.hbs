{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}

{{> header}}
    {{> "site-nav"}}
    <!-- <div class="inner">
        <div class="site-header-content">
            <h1 class="site-title">
                {{#if @site.logo}}
                    <img class="site-logo" src="{{img_url @site.logo size="l"}}" alt="{{@site.title}}" />
                {{else}}
                    {{@site.title}}
                {{/if}}
            </h1>
            <h2 class="site-description">{{@site.description}}</h2>
        </div>

    </div> -->
</header>

{{!-- The main content area --}}

<main role="main" class="container">
    <br><br>

    <div class="row">
        <div class="col-lg-2 col-md-4">
            <h6 class="sidebar-author-title">About me</h6>
            <div class="circle-avatar-small sidebar-avatar" style="background-image: url({{asset 'images/sam-texas-headshot.png'}})"></div>
            <p class="sidebar-author-desc">
                Hello there -- My name is Sam Texas, and run this joint. I am currently a CTO in digital healthcare,
                but I have a lot of other interests and projects ongoing. This site is where I talk about pretty much
                anything worth sharing.
            </p>
            <a class="sidebar-author-link" href="/about/">Learn more</a>
        </div>

        <div class="col-lg-10 col-md-8">
            <h2 class="main-title">Simple CTO</h2>
            <h4 class="page-subtitle">Whatever it is, it Does not have to be that complicated</h4>

            <div class="post-feed">

                {{#get "posts" include="tags,authors" filter="primary_tag:blog"}}
                    {{#foreach posts}}
                        {{> "post-card"}}
                    {{/foreach}}
                {{/get}}
            </div>

        </div>
        <br>
    </div>
    <br><br><br><br>
</main>
